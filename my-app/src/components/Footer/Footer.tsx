import { AppBar, Toolbar } from '@mui/material';
import styles from './Footer.module.css';

const Footer = (): JSX.Element => (
  <div className={styles.footer}>All Api provided by https://exchangerate.host/</div>
);

export default Footer;
